//import palette form
//import color cards
import React from 'react';
import './ColorContainer.scss';

const ColorContainer = () => {
    return(
        <div>
            //this is where the color cards go
            <button>GENERATE COLORS</button>
        </div>
    )
}

export default ColorContainer;